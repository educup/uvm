from typer.testing import CliRunner
from uvm.main import app

runner = CliRunner()


def test_uvm():
    # result = runner.invoke(app)
    # assert result.exit_code == 0
    # assert "Hello World!" in result.stdout
    assert True
